package tools

import "asashishi-agent/global"

var Exit string = global.GetStyledSystemComent("-- Exit")
var PressEnterToBackToChat string = global.GetStyledSystemComent("-- Press 'enter' to back to chat")
